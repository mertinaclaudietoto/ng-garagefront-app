import { FormatnumberpipePipe } from './formatnumberpipe.pipe';

describe('FormatnumberpipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatnumberpipePipe();
    expect(pipe).toBeTruthy();
  });
});
