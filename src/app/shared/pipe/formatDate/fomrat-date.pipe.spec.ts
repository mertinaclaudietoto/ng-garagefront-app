import { FomratDatePipe } from './fomrat-date.pipe';

describe('FomratDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FomratDatePipe();
    expect(pipe).toBeTruthy();
  });
});
